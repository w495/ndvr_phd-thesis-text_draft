%& -shell-escape
% !TeX root = thesis-abstract.tex
% !TeX encoding = UTF-8
% !TeX program = XeLaTeX

\documentclass{.local/lib/classes/ndvr-super-article-standalone}

\begin{document}
    \begin{content-item}{Глава про поиск}
        \textbf{В третьей главе}  описана схема поиска 
        событий по видео. Введено понятие дескриптора съёмки.
        Сформулирован алгоритм индексации и поиска
        нечётких дубликатов видео.

        \relimport{relative-distance}
        \relimport{shot-descriptor}
        \relimport{isax}
        \relimport{final-algorithm}

    \end{content-item}
\end{document}
